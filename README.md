# ARMSim 1.91 for Linux

A basic collection of ported file/collections for using ARMSim on Linux.

## Usage

This code will run on the mono framework, which should come with most Linux
distributions. Run this code with:

```
mono ARMSim.exe
```
